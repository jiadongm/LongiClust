% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/generateRP.R
\name{plot.random_projections}
\alias{plot.random_projections}
\title{Plot Method for Random Projections}
\usage{
\method{plot}{random_projections}(
  x,
  type = c("both", "basis", "projections"),
  which_projection = 1,
  which_dimension = NULL,
  which_basis = NULL,
  col_basis = NULL,
  col_proj = NULL,
  lwd = 2,
  ...
)
}
\arguments{
\item{x}{Object of class random_projections from generateRP()}

\item{type}{Character. Type of plot: "both" (default), "basis", or "projections"}

\item{which_projection}{Integer. Which projection to visualize (default: 1)}

\item{which_dimension}{Integer vector. Which dimensions to show for projections
(default: all if d0 <= 4, otherwise 1:4)}

\item{which_basis}{Integer vector. Which basis functions to plot (default: all if r <= 10)}

\item{col_basis}{Character vector or function. Colors for basis functions}

\item{col_proj}{Character vector or function. Colors for projection functions}

\item{lwd}{Numeric. Line width (default: 2)}

\item{...}{Additional arguments passed to plotting functions}
}
\description{
Plot Method for Random Projections
}
\examples{
\dontrun{
  Q_B <- generateRP(B = 50, d0 = 3, r = 8, basis = "fourier")

  # Plot both basis and projection functions
  plot(Q_B)

  # Plot only basis functions
  plot(Q_B, type = "basis")

  # Plot only projection functions
  plot(Q_B, type = "projections")

  # Customize which projection and dimensions to show
  plot(Q_B, which_projection = 5, which_dimension = 1:2)
}

}
